# Correção de build — Supabase Auth

Foram adicionados ao pacote local `vendor/lucide-react` os exports ausentes:

- `Mail`
- `KeyRound`

Todos os imports de `lucide-react` do projeto foram comparados com os exports locais e não há mais ícones ausentes.
